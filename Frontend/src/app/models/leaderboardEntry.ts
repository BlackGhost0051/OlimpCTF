export default interface LeaderboardEntry {
  rank: number;
  login: string;
  name: string;
  lastname: string;
  icon: string;
  totalPoints: number;
  completedTasks: number;
  createdAt: string;
}
